# Kong Admin API URL (using existing Kong instance)
# Use host.docker.internal to access host machine from inside container
KONG_ADMIN_URL=http://host.docker.internal:8006

# JWT Token expiration time in seconds (1 year = 31536000 seconds)
JWT_EXPIRATION_SECONDS=31536000

# Service Configuration
PORT=8000
HOST=0.0.0.0
RELOAD=true

# For development with full compose (Kong + Auth Service)
# Use PORT=8002 to avoid conflict with Kong Gateway (port 8000)
# PORT=8002

# Casdoor Configuration
CASDOOR_ENDPOINT=https://your-endponit
CASDOOR_CLIENT_ID=your-id
CASDOOR_CLIENT_SECRET=your-id
CASDOOR_ORG_NAME=ogname
CASDOOR_APP_NAME=app-name